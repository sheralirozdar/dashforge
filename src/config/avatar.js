import React from 'react'

const Avatar = () => {

    // const avatars = ["Blue_icon", "red-icon", "yellow-icon", "green-icon"]; 
    // const avatar = avatars[Math.floor(Math.random() * avatars.length)];

    return(
        <img className="avatar-initial rounded-circle" src={"./assets/icons/avatars/red-icon.png"}/>
    )
}

export default Avatar;